import 'package:flutter/material.dart';
import 'package:login_app/Screens/Validasi/components/body.dart';

class ValidasiPage extends StatelessWidget {
  const ValidasiPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
